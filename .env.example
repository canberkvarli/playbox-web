RESEND_API_KEY=
PARTNER_INBOX=canberkvarli@gmail.com

# --- Ops console (internal operator dashboard) ---
# Supabase project URL (server-only).
SUPABASE_URL=
# Service-role key — SERVER ONLY. Never expose to the browser / NEXT_PUBLIC_*.
# Grants full DB access; used solely to call op_* SECURITY DEFINER functions.
SUPABASE_SERVICE_ROLE_KEY=

# Operator login password (gates the console).
OPS_PASSWORD=
# HMAC secret for signed session cookies. Use 32+ random bytes,
# e.g. `openssl rand -hex 32`.
OPS_SESSION_SECRET=
